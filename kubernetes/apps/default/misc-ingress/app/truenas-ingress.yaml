---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: truenas
  namespace: default
spec:
  ingressClassName: nginx
  rules:
    - host: &host "truenas.${SECRET_DOMAIN}"
      http:
        paths:
          - pathType: Prefix
            path: "/"
            backend:
              service:
                name: truenas
                port:
                  number: 80
  tls:
    - hosts:
        - *host

---
apiVersion: v1
kind: Endpoints
metadata:
  name: truenas
  namespace: default
subsets:
  - addresses:
      - ip: ${TRUENAS_IP}
    ports:
      - name: http
        port: 80
        protocol: TCP

---
apiVersion: v1
kind: Service
metadata:
  name: truenas
  namespace: default
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
